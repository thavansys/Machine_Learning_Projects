# IPL_Prediction_Model_Training
This project trains a machine learning model to predict IPL match outcomes using historical match data. It includes data preprocessing, feature encoding, model training, and evaluation using Python and scikit-learn. The focus is on understanding the ML training workflow rather than deployment.
